node_modules/
relay.db
relay.db-journal
*.db-wal
.env
.env.*
.env*.local
# ...but the template is documentation and must be committed
!.env.example
*.log

# web client build output (the Worker/relay serves this, it is not source)
apps/web/dist/
apps/web/.vite/
*.tsbuildinfo

# scratch files
.tmp-*
.ai-cache.json

# wrangler: local build output, local secret file (never commit secrets)
.wrangler/
.dev.vars
.dev.vars.*
